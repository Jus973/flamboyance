# Python dependencies live in pyproject.toml.
#
#   pip install -e .
#   pip install -e ".[dev]"    # pytest, pytest-asyncio
#
# The VS Code extension under extension/ uses npm; see extension/package.json.
